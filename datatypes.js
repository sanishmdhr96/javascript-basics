// primitive data types
let fruit = 'apple'
let result = 'fail'
let number = 2
let boolean = true / false

let name = undefined;


// non - primitive data types
let object = {
    name: "sanish",
    number: 1111,
    isTrue: true,
}

//string => group of characters sanish
/**
 * "dsad ", 'dasd ', `  `
 *
 * 
 * Number => numeric value , integer, floating points
 *  
 * Boolean => true, false, 1/0
 * 
 * undefined => declared for no value
 * 
 *  
 */

let subject = {
    name: 'physics',
    marks: 100
};

subject.name;
subject.marks

let colors = null;

// <!-- --> console.log('subject', subject)
console.log('colors', colors)

